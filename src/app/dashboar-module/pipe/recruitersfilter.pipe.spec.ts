import { RecruitersfilterPipe } from './recruitersfilter.pipe';

describe('RecruitersfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecruitersfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
