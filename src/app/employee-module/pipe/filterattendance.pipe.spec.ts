import { FilterattendancePipe } from './filterattendance.pipe';

describe('FilterattendancePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterattendancePipe();
    expect(pipe).toBeTruthy();
  });
});
