import { TodayattendancePipe } from './todayattendance.pipe';

describe('TodayattendancePipe', () => {
  it('create an instance', () => {
    const pipe = new TodayattendancePipe();
    expect(pipe).toBeTruthy();
  });
});
