import { LeavecountPipe } from './leavecount.pipe';

describe('LeavecountPipe', () => {
  it('create an instance', () => {
    const pipe = new LeavecountPipe();
    expect(pipe).toBeTruthy();
  });
});
