import { AttendanceFilterPipe } from './attendance-filter.pipe';

describe('AttendanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AttendanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
