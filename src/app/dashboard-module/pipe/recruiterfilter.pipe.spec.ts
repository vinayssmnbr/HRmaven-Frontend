import { RecruiterfilterPipe } from './recruiterfilter.pipe';

describe('RecruiterfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecruiterfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
