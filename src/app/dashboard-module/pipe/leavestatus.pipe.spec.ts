import { LeavestatusPipe } from './leavestatus.pipe';

describe('LeavestatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LeavestatusPipe();
    expect(pipe).toBeTruthy();
  });
});
