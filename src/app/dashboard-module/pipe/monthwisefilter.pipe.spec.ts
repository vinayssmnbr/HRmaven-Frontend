import { MonthwisefilterPipe } from './monthwisefilter.pipe';

describe('MonthwisefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthwisefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
