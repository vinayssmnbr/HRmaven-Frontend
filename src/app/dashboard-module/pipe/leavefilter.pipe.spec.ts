import { LeavefilterPipe } from './leavefilter.pipe';

describe('LeavefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeavefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
