---
image: node:14-alpine
pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install -g @angular/cli@10.1.3
    - npm install
    - ng build --configuration production
  artifacts:
    paths:
      - public
  only:
    - develop
